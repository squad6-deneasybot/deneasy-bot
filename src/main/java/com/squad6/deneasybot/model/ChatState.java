package com.squad6.deneasybot.model;

public enum ChatState {
    START,
    AWAITING_APP_KEY,
    AWAITING_APP_SECRET,
    AWAITING_EMAIL,
    AWAITING_EMAIL_CODE,
    AUTHENTICATED,
    AWAITING_POST_ACTION,
    AWAITING_CRUD_MENU_CHOICE,
    AWAITING_FAQ_CHOICE,
    AWAITING_CRUD_ADD_NAME,
    AWAITING_CRUD_ADD_EMAIL,
    AWAITING_CRUD_ADD_PHONE,
    AWAITING_CRUD_REMOVE_EMAIL_CONFIRM,
    AWAITING_CRUD_UPDATE_SELECT_USER,
    AWAITING_CRUD_UPDATE_SELECT_FIELD,
    AWAITING_CRUD_UPDATE_NEW_VALUE,
    AWAITING_CRUD_POST_ACTION,
    AWAITING_WISHLIST,
    AWAITING_FEEDBACK_TEXT,
    AWAITING_FEEDBACK_RATING,
    AWAITING_REPORT_PERIOD_CHOICE,
    AWAITING_REPORT_CUSTOM_DAYS,
    AWAITING_SUBSCRIPTION_FREQUENCY,
}